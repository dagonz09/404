<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="/css/common.css" media="screen"/>
        <link rel="stylesheet" href="/css/listar_usuarios.css" media="screen"/>
    </head>
    <body>
        <%- include("header") %>
        <main>
            <form action="" method="get">
                <input type="submit" value="Busqueda" class="button"> 
                <input type="search" name="busqueda" placeholder="Buscar por titulo/cuerpo de pregunta" id="busqueda">
            </form>

            <form action="buscarUsuario" method="post">
                <input type="submit" value="Busqueda" class="button">
                <input type="search" name="busqueda" placeholder="Filtrar por usuario" id="busqueda">
            </form>


            <h1>Todos los usuarios</h1>
            <div class="primeraLinea">
                <h2><%= users.length %> usuarios </h2>
                
            </div>
            <ul class="cuadricula_listar_usuarios">
                <% users.forEach(function(user) { %>
                <li >
                    <div>
                        <img src=/img/<%= user.imagen %> alt=""  class="foto_pequeña">
                        <h3><%= user.nombre %></h3>
                        <p>100</p>
                    </div>
                </li>
                <% }); %>
            </ul>
        </main>
    </body>
</html>